----- 2022-12-06 -----

- надо было использовать ant.d - это есть в задании

- если используешь редакс - надо было persist storage делать, а не юзать local storage

- надо было генерить хуки с помошью graphql codegen, а не использовать редакс

- props использованы неправильно - это пропсы функционального компонента. функциональный компонент создается через generic React.FC<props>