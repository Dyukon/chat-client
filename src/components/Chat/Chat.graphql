query GetCurrentUserInfo {
  currentUser {
    id
    name
  }
  messages(params: {

  }) {
    id
    text
    createdAt
    senderId
    senderName
  }
}

mutation SendMessage($text: String!) {
  createMessage(params: {
    text: $text
  }) {
    id
    text
  }
}