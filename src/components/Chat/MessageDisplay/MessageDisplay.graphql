query GetEvents {
  events(params: {}) {
    id
    type
    createdAt
    senderId
    senderName
    message
  }
}

subscription OnEventAdded {
  eventAdded {
    id
    type
    createdAt
    senderId
    senderName
    message
  }
}